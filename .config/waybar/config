{
    "layer": "top",
    "position": "top",
    "height": 25,
    "modules-left": ["sway/workspaces"],
    "modules-center": ["sway/window"],
    "modules-right": ["custom/lang", "tray", "idle_inhibitor", "pulseaudio", "network", "cpu", "memory", "temperature", "battery", "clock"],
    "sway/workspaces": {
        "disable-scroll": true,
        "disable-click": true,
    },
    "tray": {
        "spacing": 10,
    },
    "clock": {
        "format": "{:%R | %d %b %Y}"
    },
    "cpu": {
        "format": " {usage}%"
    },
    "memory": {
        "format": " {}%"
    },
    "temperature": {
        "critical-threshold": 75,
        "format": " {temperatureC}°C"
    },
    "battery": {
        "bat": "BAT0",
        "interval": 60,
        "full-at": 95,
        "states": {
            "good": 95,
            "warning": 40,
            "critical": 20
        },
        "format": "{icon} {capacity}%",
        "format-icons": ["", "", "", "", ""]
    },
    "network": {
        "format-wifi": " {essid}",
        "format-disconnected": "  Disconnected ",
        "on-click": "nmcli dev wifi rescan && gnome-terminal -- zsh -c nmtui"
    },
    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-muted": "",
        "format-icons": {
            "headphones": "",
            "default": ["", ""]
        },
        "on-click": "pavucontrol"
    },
    "idle_inhibitor": {
        "format": "{icon}",
        "format-icons": {
            "activated": "",
            "deactivated": ""
        }
    },
    "custom/lang": {
      "exec": "swaymsg -t get_inputs | grep layout_name | grep -v En | awk 'END{if(NR == 0){print \"EN\"}else{print toupper(substr($2,2,2))}}'",
      "interval": 1,
      "format": " {}"
    },
}
